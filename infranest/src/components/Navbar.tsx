// This file is no longer needed as we've replaced it with Sidebar and Header components
// Keeping it empty to avoid breaking imports, but it won't be used
import React from 'react';

const Navbar: React.FC = () => {
  return null;
};

export default Navbar;